# 10clouds_task
POC for one the selected business problems for 10clouds recrutiment task.

In order for the project to run one must create a .env file with an OpanAI key inside and install openai, dotenv and streamlit packages using pip.
To run execute the project one has to type streamlit run main.py and the app will pop up inside a browser.

