# 10clouds_task
POC for one the selected business problems for 10clouds recrutiment task.

In order for the project to run one must create a .env file with an OpanAI key inside and run pip install openai and pip install dotenv.

